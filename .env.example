# Flask session signing key. The in-code dev fallback is used when this is
# unset, so the app still boots with zero configuration. Set a strong random
# value for any non-demo deployment.
SECRET_KEY=

# HTTP port. 8050 is Dash's conventional port; honour the platform's PORT
# environment variable instead when it is present.
PORT=8050
